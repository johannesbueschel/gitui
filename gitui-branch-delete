#!/bin/zsh

# TODO: Make this a proper bash script

if [ -n "$1" ]
then
    read -r -k 1 "response?Do you really want to delete '$1'? "
    echo    # (optional) move to a new line

    if [[ "$response" =~ ^[Yy]$ ]]
    then
        git branch -d "$1"
    fi
fi
